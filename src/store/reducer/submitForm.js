import { createSlice } from "@reduxjs/toolkit";

const initialState ={
    name:'user',
    userForm={},
    user: null,
}

const onChange = () =>{

}
const onSUbmit = () =>{

}

export const submitForm = createSlice({
    name:"submitForm",
    initialState,
    reducer:{

    }

})